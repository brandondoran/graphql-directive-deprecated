language: node_js
node_js:
  - 6
  - 8
  - 10
cache:
  directories:
    - node_modules
notifications:
  email: false
script:
  - npm run lint
  - npm run coverage
  - npm run build
after_script:
  # send the coverage report to coveralls
  - pwd
  - ls -la
  - ls -la coverage
  - 'cat coverage/lcov.info | npx coveralls'
